apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: platform-engine-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: platform-engine.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: platform-engine-service
            port:
              number: 80
